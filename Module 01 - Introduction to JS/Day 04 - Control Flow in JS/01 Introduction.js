//Normally, a JS programs gets executed top-to-bottom (not always left-to-right). But we can change that with certain statements:
    //a) if
    //b) if-else
    //c) switch-case
    //d) break
    //e) continue
    //f) etc.
        //NOTE: there's no 'goto' in JS.