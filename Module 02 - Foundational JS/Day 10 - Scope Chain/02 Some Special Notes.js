//Some Interesting Notes on Scope in JS:
    //a) Whenever we declare a variable with 'var' in the global object, it is added as a property to the global object.
    //b) A 'var' variable can only be either global or function scoped. There's no block-scoping in 'var'.